{
  // OpenCode MCP configuration for {{ project_name }}
  "mcpServers": {
    "abstract-mcp": {
      "command": "poetry",
      "args": ["run", "abstract-mcp", "serve"],
      "cwd": "{{ root }}"
    }
  }
}
